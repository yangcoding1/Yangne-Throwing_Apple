# Apple Toss Minimal — Tistory 스킨 적용 가이드

## 목차
1. [스킨 파일 구성](#1-스킨-파일-구성)
2. [현재 스킨 백업](#2-현재-스킨-백업-필수)
3. [스킨 업로드 방법](#3-스킨-업로드-방법)
4. [스킨 옵션 설정](#4-스킨-옵션-설정)
5. [적용 후 검증](#5-적용-후-검증)
6. [알려진 제한사항](#6-알려진-제한사항)
7. [커스터마이징 팁](#7-커스터마이징-팁)

---

## 1. 스킨 파일 구성

스킨은 다음 3개 파일로 구성됩니다.

```
skin.html    — 블로그 HTML 템플릿 (Tistory 치환자 포함)
style.css    — 전체 스타일시트 (CSS 변수, 다크모드, 반응형)
index.xml    — 스킨 메타정보 및 관리자 옵션 정의
```

### 기능 요약

| 기능 | 설명 |
|------|------|
| 라이트/다크 모드 | 헤더 우측 아이콘 클릭으로 전환, 새로고침 후에도 유지 |
| OS 테마 연동 | 사용자가 별도 선택 전까지 OS 다크모드 설정을 자동 반영 |
| 읽기 진행 바 | 헤더 하단에 스크롤 진행도를 2px 바로 표시 |
| 플로팅 목차 (TOC) | 포스트에 H2/H3 제목이 2개 이상이면 사이드바에 자동 생성, 스크롤 연동 |
| 반응형 레이아웃 | 768px 이하 모바일: 1컬럼, 사이드바는 슬라이드 패널로 전환 |
| 히어로 섹션 | 메인 페이지(index)에서만 표시, 글라스모피즘 프로필 카드 포함 |
| 검색 | 헤더 검색 아이콘 클릭 시 드롭다운 검색창 표시 |

---

## 2. 현재 스킨 백업 (필수)

**스킨을 바꾸면 기존 커스터마이징이 모두 초기화됩니다. 반드시 먼저 백업하세요.**

1. Tistory 관리자 접속 → **꾸미기** → **스킨 편집**
2. 우측 상단 **스킨 보관함** 클릭
3. 현재 스킨 옆 **보관** 버튼 클릭 → 이름 입력 후 저장
4. 보관된 스킨은 나중에 복원할 수 있습니다

---

## 3. 스킨 업로드 방법

### 방법 A — 파일 직접 편집 (권장)

1. Tistory 관리자 → **꾸미기** → **스킨 편집**
2. 우측 **HTML 편집** 클릭
3. `skin.html` 파일 내용을 전체 선택 후 붙여넣기
4. **저장** 클릭
5. 상단 탭에서 **CSS** 클릭
6. `style.css` 파일 내용을 전체 선택 후 붙여넣기
7. **저장** 클릭

> `index.xml`은 스킨 업로드 시 자동 적용됩니다. 파일 편집기로 직접 수정할 경우:  
> **파일 업로드** 탭 → `index.xml` 선택 → 업로드

### 방법 B — ZIP 파일 업로드

1. `skin.html`, `style.css`, `index.xml` 을 하나의 ZIP 파일로 압축
   ```
   apple-toss-minimal.zip
   ├── skin.html
   ├── style.css
   └── index.xml
   ```
2. Tistory 관리자 → **꾸미기** → **스킨** → **스킨 직접 올리기**
3. ZIP 파일 선택 후 업로드
4. 업로드 완료 후 **적용** 클릭

> **주의**: ZIP 업로드 시 `index.xml`의 설정값이 초기화될 수 있습니다.  
> 업로드 후 스킨 옵션을 다시 확인하세요.

---

## 4. 스킨 옵션 설정

스킨 적용 후 관리자에서 히어로 배경 이미지를 설정할 수 있습니다.

1. Tistory 관리자 → **꾸미기** → **스킨 편집**
2. 우측 **옵션** 탭 클릭
3. 아래 옵션 설정:

| 옵션명 | 설명 | 권장값 |
|--------|------|--------|
| 히어로 배경 이미지 | 메인 페이지 상단 배경 이미지 | 1920×600px 이상, 어두운 계열 이미지 권장 |

> 이미지를 설정하지 않으면 기본 그라데이션 배경이 표시됩니다.

### 블로그 기본 설정 (별도 메뉴)

히어로 카드에 표시되는 정보는 블로그 기본 설정에서 수정합니다:

- **관리자** → **블로그** → **기본 설정**
  - 블로그 제목: 헤더에 표시
  - 블로그 설명: 히어로 카드 한 줄 소개
  - 블로그 대표 이미지: 히어로 카드 프로필 사진 + 사이드바 프로필

---

## 5. 적용 후 검증

스킨 적용 후 아래 체크리스트를 순서대로 확인하세요.

### 5-1. 치환자 노출 확인 (가장 중요)

브라우저에서 페이지 소스 보기(`Ctrl+U` / `Cmd+U`)를 열어  
`[##_` 문자열을 검색합니다. **하나도 없어야 정상**입니다.

```
검색어: [##_
결과: 0개 → 정상
결과: 1개 이상 → skin.html 붙여넣기 오류, 다시 시도
```

### 5-2. 페이지별 렌더링 확인

| 페이지 | 확인 항목 |
|--------|----------|
| 메인 (index) | 글 목록 카드 표시, 히어로 섹션 표시 |
| 포스트 상세 | 본문 내용 표시, H2/H3 있으면 TOC 표시 |
| 카테고리/태그 | 글 목록 표시, 제목 및 건수 표시 |
| 검색 결과 | 검색어 및 결과 표시 |

### 5-3. 다크모드 플래시 확인 (테마 전환)

1. 헤더 달/해 아이콘 클릭 → 다크모드 전환 확인
2. 페이지 새로고침 → 다크모드 유지되는지 확인 (플래시 없어야 함)
3. Chrome DevTools → Network → Slow 3G 설정 후 새로고침  
   → 첫 화면에서 흰 배경이 번쩍이지 않아야 함

### 5-4. 반응형 확인

| 뷰포트 | 확인 항목 |
|--------|----------|
| 1280px | 2컬럼(본문+사이드바) 레이아웃 |
| 768px | 1컬럼, 상단 햄버거 메뉴 표시 |
| 360px | 카드 이미지 풀너비, 글자 잘림 없음 |

Chrome DevTools → Toggle Device Toolbar(`Ctrl+Shift+M`)로 테스트.

### 5-5. 키보드 접근성 확인

`Tab` 키만으로 헤더 메뉴, 검색, 테마 토글, 글 카드를 순서대로 탐색할 수 있어야 합니다.

---

## 6. 알려진 제한사항

### 폰트 CDN 의존성

현재 Pretendard 폰트를 `cdn.jsdelivr.net`에서 로드합니다.  
CDN 장애 시 시스템 기본 폰트(Apple SD Gothic Neo, Noto Sans KR 등)로 자동 대체됩니다.

**자체 호스팅으로 전환하려면 (선택사항)**:

1. [Pretendard 공식 릴리즈](https://github.com/orioncactus/pretendard/releases)에서 Variable 웹폰트 다운로드
2. Tistory 스킨 편집 → **파일 업로드** 탭 → `pretendardvariable.min.css` + woff2 파일 업로드
3. `skin.html`의 CDN `<link>` 태그를 아래로 교체:
   ```html
   <link rel="stylesheet" href="./images/pretendardvariable.min.css">
   ```
4. CSS 내부 `url()` 경로를 업로드된 파일 경로로 수정

### 댓글 스타일

현재 `[##_comment_group_##]` (Tistory React 댓글 시스템)을 사용합니다.  
댓글 UI는 Tistory가 자체적으로 렌더링하므로 스킨 CSS에서 세부 제어에 제한이 있습니다.  
댓글 영역에 추가 스타일을 적용하려면 Tistory 제공 CSS 클래스(`tt-comment-cont` 등)를 타겟팅하세요.

### Tistory 플랫폼 업데이트

Tistory가 치환자 체계를 변경하면 일부 기능이 동작하지 않을 수 있습니다.  
특히 `[##_body_id_##]` 값이 변경되면 히어로 섹션 표시 조건이 영향을 받을 수 있습니다.

---

## 7. 커스터마이징 팁

### 색상 변경

`style.css` 최상단 `:root` 블록의 CSS 변수만 수정하면 전체 색상이 변경됩니다.

```css
:root {
  --color-accent: #007AFF;   /* 강조색 (버튼, 링크, 카테고리 칩) */
}

[data-theme="dark"] {
  --color-accent: #3182F6;   /* 다크모드 강조색 */
}
```

### 레이아웃 폭 조정

```css
:root {
  --sidebar-w: 272px;   /* 사이드바 너비 */
}

.site-wrapper {
  max-width: 1200px;    /* 최대 컨텐츠 폭 */
}
```

### 히어로 높이 변경

```css
.hero-section {
  height: 420px;   /* 원하는 높이로 변경 */
}
```

### 글 카드에서 썸네일 숨기기

```css
.post-card-thumb { display: none; }
```

### 사이드바 완전히 숨기기

```css
.site-wrapper {
  grid-template-columns: 1fr;
}

.site-sidebar { display: none; }
```

---

## 문의 및 이슈

스킨 적용 중 문제가 발생하면 다음을 먼저 확인하세요:

1. `[##_` 치환자가 화면에 노출되는 경우 → skin.html 전체를 다시 붙여넣기
2. 스타일이 깨지는 경우 → style.css 전체를 다시 붙여넣기, 브라우저 캐시 삭제(`Ctrl+Shift+R`)
3. 히어로가 안 보이는 경우 → 현재 페이지가 메인(index)인지 확인
4. TOC가 안 보이는 경우 → 포스트 본문에 `<h2>` 또는 `<h3>` 태그가 2개 이상인지 확인
