# Tistory 스킨 스터디 가이드

> **Apple Toss Minimal** 스킨을 예제로 삼아 `index.xml`, `skin.html`, `style.css` 세 파일이 어떻게 동작하는지 학습합니다.

---

## 목차

1. [파일 구조 한눈에 보기](#1-파일-구조-한눈에-보기)
2. [index.xml — 스킨 설정 파일](#2-indexxml--스킨-설정-파일)
3. [skin.html — 템플릿 파일](#3-skinhtml--템플릿-파일)
4. [style.css — 스타일 파일](#4-stylecss--스타일-파일)
5. [세 파일이 연결되는 흐름](#5-세-파일이-연결되는-흐름)

---

## 1. 파일 구조 한눈에 보기

```
TSTORY_SKIN/
├── index.xml       ← 스킨 메타데이터 · 기본값 · 사용자 변수 정의
├── skin.html       ← 페이지 HTML 뼈대 + Tistory 전용 태그
└── style.css       ← 전체 스타일 (CSS 변수 → 컴포넌트 → 반응형)
```

Tistory는 블로그 접속 시 이 세 파일을 조합해 HTML 페이지를 완성합니다.  
`[##_변수_##]`와 `<s_태그>` 부분을 실제 데이터로 치환한 뒤 브라우저에 전달합니다.

---

## 2. index.xml — 스킨 설정 파일

### 역할

Tistory 관리자 패널이 스킨을 인식하기 위한 **설정 명세서**입니다.  
스킨 이름, 기본 표시 개수, 카테고리 트리 색상, 사용자가 UI에서 직접 바꿀 수 있는 변수를 정의합니다.

### 구조 분해

```xml
<skin>
  <!-- ① 스킨 기본 정보 -->
  <information>
    <name>Apple Toss Minimal</name>
    <version>1.0</version>
    <description>...</description>
  </information>

  <!-- ② 기본값 설정 -->
  <default>
    <recentEntries>5</recentEntries>      <!-- 최근 글 표시 개수 -->
    <showListOnCategory>1</showListOnCategory> <!-- 카테고리 클릭 시 목록 표시 여부 -->
    <contentWidth>800</contentWidth>       <!-- 본문 최대 너비 (px) -->

    <!-- 카테고리 위젯 트리 색상 (CSS와 연동) -->
    <tree>
      <color>1D1D1F</color>           <!-- 일반 텍스트 색 -->
      <activeColor>007AFF</activeColor> <!-- 선택된 항목 색 -->
      <activeBgColor>EAF3FF</activeBgColor> <!-- 선택된 항목 배경 -->
    </tree>
  </default>

  <!-- ③ 사용자 정의 변수 (관리자 UI에서 값 입력 가능) -->
  <variables>
    <variablegroup name="히어로 이미지">
      <variable>
        <name>cover-image</name>         <!-- skin.html에서 [##_var_cover-image_##] 로 참조 -->
        <type>IMAGE</type>               <!-- 입력 타입: IMAGE / TEXT / COLOR -->
        <default></default>
      </variable>
    </variablegroup>
  </variables>
</skin>
```

### 핵심 포인트

| 태그 | 설명 |
|------|------|
| `<name>` | 관리자 패널에 표시되는 스킨 이름 |
| `<recentEntries>` | `[##_rct_entry_##]` 위젯의 기본 표시 개수 |
| `<showListOnCategory>1` | `1` = 카테고리 클릭 시 `s_list` 섹션 렌더링 |
| `<tree>` | `[##_category_##]` 위젯의 기본 색상 지정 |
| `<variable>` | skin.html 안에서 `[##_var_변수명_##]` 으로 사용 |

---

## 3. skin.html — 템플릿 파일

### Tistory 전용 문법 2가지

Tistory는 일반 HTML에 두 가지 전용 문법을 더합니다.

#### ① 치환 변수 `[##_이름_##]`

Tistory 서버가 렌더링 시 실제 값으로 **치환**합니다.

```html
<!-- 예시 -->
[##_title_##]              → 블로그 제목
[##_blog_link_##]          → 블로그 URL
[##_article_rep_title_##]  → 글 제목 (반복 섹션 안에서만 유효)
[##_body_id_##]            → 현재 페이지 타입 ID (tt-body-index 등)
[##_var_cover-image_##]    → index.xml에서 정의한 사용자 변수
```

#### ② 조건/반복 태그 `<s_태그>`

특정 조건일 때만 렌더링되거나 데이터를 반복합니다.

```html
<s_태그>
  <!-- 조건이 맞으면 이 안쪽이 출력됨 -->
</s_태그>
```

### 주요 `<s_>` 태그 목록

| 태그 | 렌더링 조건 |
|------|------------|
| `<s_t3>` | 전체 스킨 영역 감싸기 (항상 필요) |
| `<s_article_rep>` | 글 목록 또는 글 상세 페이지일 때 |
| `<s_index_article_rep>` | 홈·카테고리 등 **목록** 페이지일 때 (반복) |
| `<s_permalink_article_rep>` | **단일 글** 상세 페이지일 때 |
| `<s_list>` | 카테고리·태그·검색 결과 목록 페이지일 때 |
| `<s_list_rep>` | `s_list` 안에서 글 하나씩 반복 |
| `<s_paging>` | 페이지네이션이 필요할 때 |
| `<s_sidebar>` | 사이드바 위젯 묶음 |
| `<s_sidebar_element>` | 사이드바 위젯 하나 |
| `<s_notice_rep>` | 공지사항이 있을 때 |
| `<s_rp_count>` | 댓글 수가 1개 이상일 때 |
| `<s_tag_label>` | 태그가 있을 때 |
| `<s_ad_div>` | 블로그 관리자(본인)로 로그인했을 때 |
| `<s_if_var_변수명>` | index.xml 변수에 값이 설정됐을 때 |

### 페이지 타입별 body ID

`<body id="[##_body_id_##]">` 에 들어오는 값으로 CSS에서 페이지별 스타일을 다르게 적용합니다.

| body id | 페이지 |
|---------|--------|
| `tt-body-index` | 홈 (전체 글 목록) |
| `tt-body-category` | 카테고리별 목록 |
| `tt-body-tag` | 태그별 목록 |
| `tt-body-search` | 검색 결과 |
| `tt-body-archive` | 날짜별 아카이브 |
| `tt-body-entry` | 단일 글 상세 |
| `tt-body-guestbook` | 방명록 |

### skin.html 구조 지도

```
<body id="[##_body_id_##]">
<s_t3>

  ── HEADER (.site-header) ──────────────────────────────
  | 로고 / GNB 메뉴 / 검색 / 테마 토글 / 모바일 버튼      |
  ──────────────────────────────────────────────────────

  ── HERO (.hero-section) ───────────────────────────────
  | #tt-body-index 일 때만 CSS로 표시                     |
  | [##_title_##], [##_desc_##]                          |
  ──────────────────────────────────────────────────────

  ── .site-wrapper (2-column grid) ──────────────────────
  |                                                      |
  |  MAIN (.main-content)          SIDEBAR               |
  |  ┌────────────────────┐       ┌──────────────────┐  |
  |  │ s_notice_rep       │       │ 프로필 위젯       │  |
  |  │ s_article_rep      │       │ 카테고리 위젯     │  |
  |  │  ├ s_index_article │       │ TOC 위젯 (JS)    │  |
  |  │  └ s_permalink     │       └──────────────────┘  |
  |  │ s_list             │                              |
  |  │ s_paging           │                              |
  |  │ s_tag / s_guest    │                              |
  |  └────────────────────┘                              |
  ──────────────────────────────────────────────────────

  ── FOOTER (.site-footer) ──────────────────────────────

</s_t3>
```

### 주요 치환 변수 빠른 참조

#### 글 목록 (`s_index_article_rep` 안)

| 변수 | 출력 내용 |
|------|----------|
| `[##_article_rep_link_##]` | 글 URL |
| `[##_article_rep_title_##]` | 글 제목 |
| `[##_article_rep_summary_##]` | 글 요약 |
| `[##_article_rep_simple_date_##]` | 날짜 (짧은 형식) |
| `[##_article_rep_category_##]` | 카테고리명 |
| `[##_article_rep_rp_cnt_##]` | 댓글 수 (**이 섹션에서만 유효**) |
| `[##_article_rep_like_cnt_##]` | 좋아요 수 (**이 섹션에서만 유효**) |
| `[##_article_rep_thumbnail_url_##]` | 썸네일 URL |

#### 카테고리/검색 목록 (`s_list_rep` 안)

| 변수 | 출력 내용 |
|------|----------|
| `[##_list_rep_link_##]` | 글 URL |
| `[##_list_rep_title_text_##]` | 글 제목 |
| `[##_list_rep_summary_##]` | 글 요약 |
| `[##_list_rep_regdate_##]` | 날짜 |
| `[##_list_rep_category_##]` | 카테고리명 |
| `[##_list_rep_thumbnail_url_##]` | 썸네일 URL |
| ❌ 댓글/좋아요 수 | **사용 불가** — `s_index_article_rep`만 지원 |

> **중요:** 댓글·좋아요 수는 `s_index_article_rep` 전용 변수입니다.  
> `s_list_rep`에서는 이 변수를 쓸 수 없어서, 홈 페이지에서 목록을 표시할 때는 반드시 `s_index_article_rep`을 사용해야 합니다.

---

## 4. style.css — 스타일 파일

### 파일 구성 (섹션 번호 기준)

| 섹션 | 내용 |
|------|------|
| 1 | CSS 변수 (`:root`, `[data-theme="dark"]`) |
| 2 | Reset + Base |
| 3–4 | 헤더 + 검색 드롭다운 |
| 5 | 읽기 진행 바 |
| 6 | 히어로 섹션 (`#tt-body-index` 스코프) |
| 7 | 2-column 레이아웃 그리드 |
| 8–9 | 공지 바 + 글 카드 |
| 10–15 | 글 상세, 본문 prose, 이전/다음, 관련 글, 댓글 |
| 16 | 리스트 페이지 헤더 (`list-page-header`) |
| 17 | 페이지네이션 |
| 18–21 | 사이드바 (공통, 프로필, 카테고리, TOC) |
| 22–23 | 푸터 + 애니메이션 |
| 24–26 | 반응형 (1024 · 768 · 480px) |
| 27–29 | 태그 클라우드, 방명록, 구독 버튼 |
| 30–31 | 페이지 타입별 중복 제거 · 홈 레이아웃 |

### CSS 변수 시스템

```css
:root {
  /* 색상 */
  --color-bg:        #FBFBFD;   /* 페이지 배경 */
  --color-surface:   #FFFFFF;   /* 카드 배경 */
  --color-text-1:    #1D1D1F;   /* 본문 텍스트 */
  --color-text-2:    #86868B;   /* 보조 텍스트 */
  --color-text-3:    #A1A1A6;   /* 연한 텍스트 */
  --color-accent:    #007AFF;   /* 포인트 색 (파란색) */
  --color-accent-bg: #EAF3FF;   /* 포인트 배경 (연한 파랑) */
  --color-border:    rgba(0,0,0,0.08);
  --color-chip-bg:   #F2F2F7;   /* 칩/배지 배경 */

  /* 그림자 */
  --shadow-card:     0 4px 20px rgba(0,0,0,0.05);
  --shadow-hover:    0 12px 40px rgba(0,0,0,0.12);

  /* 모서리 반경 */
  --radius-card:     20px;
  --radius-btn:      12px;
  --radius-chip:     100px;   /* 완전한 pill 형태 */

  /* 레이아웃 */
  --header-h:        60px;
  --sidebar-w:       272px;

  /* 인터랙션 */
  --ease:            cubic-bezier(0.4, 0, 0.2, 1);
  --dur:             0.3s;
}
```

다크 모드는 `[data-theme="dark"]` 셀렉터로 같은 변수명을 재정의합니다.

```css
[data-theme="dark"] {
  --color-bg:        #111111;
  --color-accent:    #3182F6;   /* 다크모드에서 약간 밝은 파랑 */
  --color-accent-bg: #1A2B4A;
  /* ...나머지 변수 */
}
```

### 페이지 타입별 CSS 스코프

`body id`를 활용해 **특정 페이지에서만** 스타일을 적용합니다.

```css
/* 히어로 섹션: 홈에서만 보임 */
.hero-section { display: none; }
#tt-body-index .hero-section { display: block; }

/* 홈 인덱스 페이지: "전체 글" 헤더를 글 목록 위로 올리기 */
#tt-body-index .main-content {
  display: flex;
  flex-direction: column;
}
#tt-body-index .main-content > .list-page        { order: 1; } /* 헤더 먼저 */
#tt-body-index .main-content > article.post-card { order: 2; } /* 글 카드 다음 */
#tt-body-index .main-content > .pagination       { order: 3; }

/* 카테고리·태그·검색 페이지: s_article_rep 중복 글 카드 숨기기 */
#tt-body-category .main-content > article.post-card,
#tt-body-tag      .main-content > article.post-card,
#tt-body-search   .main-content > article.post-card { display: none; }
```

### 카테고리 위젯 스타일 구조

`[##_category_##]`는 Tistory가 렌더링하는 HTML이라 구조를 직접 작성하지 않고 CSS로만 꾸밉니다.

```css
/* Tistory가 만드는 카테고리 HTML 구조 (예상) */
/*
<ul class="category_list">
  <li class="selected">              ← 현재 선택된 항목
    <a href="...">분류 전체보기 <span class="cnt">24</span></a>
  </li>
  <li>
    <a href="...">양느 <span class="cnt">4</span></a>
    <ul>                             ← 하위 카테고리
      <li><a href="...">투자 <span class="cnt">3</span></a></li>
    </ul>
  </li>
</ul>
*/

/* 선택 상태: 연한 파랑 배경 + 파란 텍스트 */
.sidebar-category .selected > a {
  background: var(--color-accent-bg);  /* #EAF3FF */
  color: var(--color-accent);          /* #007AFF */
  font-weight: 700;
}

/* 하위 카테고리: 좌측 세로선으로 트리 구조 표현 */
.sidebar-category ul ul {
  margin-left: 6px;
  padding-left: 12px;
  border-left: 1.5px solid var(--color-border);
}
```

### 다크 모드 구현 방식

```html
<!-- skin.html: JS가 localStorage 값을 읽어 초기 테마 설정 -->
<html lang="ko" data-theme="light">
<script>
  (function () {
    var saved = localStorage.getItem('atm-theme');
    var preferred = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    document.documentElement.dataset.theme = saved || preferred;
  }());
</script>
```

```css
/* style.css: data-theme 속성으로 변수 전환 */
[data-theme="dark"] { --color-bg: #111111; /* ... */ }
[data-theme="light"] .icon-moon { display: none; }
[data-theme="dark"]  .icon-sun  { display: none; }
```

```js
// skin.html 하단 script: 버튼 클릭으로 토글
themeToggle.addEventListener('click', function () {
  var next = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
  document.documentElement.dataset.theme = next;
  localStorage.setItem('atm-theme', next);
});
```

---

## 5. 세 파일이 연결되는 흐름

```
[사용자 접속]
      │
      ▼
Tistory 서버 처리
  ① index.xml 읽기 → 기본값·변수 확인
  ② skin.html 처리 → [##_##] 치환, <s_> 조건 평가
  ③ style.css 첨부
      │
      ▼
완성된 HTML 전송
      │
      ▼
브라우저 렌더링
  ① style.css 파싱 → CSS 변수 계산
  ② JS 실행 (테마 init, 인터랙션, TOC)
```

### 자주 하는 수정과 파일 위치

| 하고 싶은 것 | 수정 파일 · 위치 |
|-------------|-----------------|
| 블로그 제목/설명 변경 | Tistory 관리자 설정 (코드 수정 불필요) |
| 히어로 배경 이미지 | `index.xml` 변수 또는 관리자 스킨 옵션 |
| 특정 섹션 보이기/숨기기 | `style.css` + body id 셀렉터 |
| 글 카드 레이아웃 변경 | `skin.html` `s_index_article_rep` + `style.css` `.post-card*` |
| 새 위젯 추가 | `skin.html` 사이드바 `s_sidebar_element` 안에 HTML 추가 |
| 색상 테마 변경 | `style.css` `:root` 의 CSS 변수값 변경 |
| 다크모드 색상 | `style.css` `[data-theme="dark"]` 변수값 변경 |
| 카테고리 트리 색 | `style.css` `.sidebar-category` + `index.xml` `<tree>` |

---

> **요약:** `index.xml`은 설정, `skin.html`은 구조(어떤 데이터를 어디에), `style.css`는 표현(어떻게 보이는지)을 담당합니다. Tistory 전용 `<s_>` 태그와 `[##_##]` 변수는 서버에서 처리되고, CSS의 body id 스코프(`#tt-body-index` 등)는 클라이언트에서 페이지 타입별 차별화를 담당합니다.
